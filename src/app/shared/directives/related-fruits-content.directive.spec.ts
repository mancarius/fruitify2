import { RelatedFruitsContentDirective } from './related-fruits-content.directive';

describe('RelatedFruitsContentDirective', () => {
  it('should create an instance', () => {
    const directive = new RelatedFruitsContentDirective();
    expect(directive).toBeTruthy();
  });
});
